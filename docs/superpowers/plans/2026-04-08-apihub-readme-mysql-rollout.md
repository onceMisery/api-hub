# ApiHub README MySQL Rewrite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite `README.md` into a MySQL-first, single-node-first, prototype-informed but not prototype-limited implementation document for ApiHub.

**Architecture:** Keep the runtime design as a lightweight modular monolith backed only by MySQL 8 for the default path. Use the existing `prototype` app as the concrete frontend reference for marketing, docs, console, debug, environment, version, and mock experiences, but explicitly document the gaps between the prototype and the target product, including higher visual polish, stronger card-based design language, and missing management capabilities such as grouping/module organization.

**Tech Stack:** Markdown, MySQL 8 syntax, Next.js 15, React 19, TypeScript 5, Tailwind CSS, shadcn/ui, Framer Motion, Spring Boot 3.2, Java 21, MySQL 8, Caffeine.

---

### Task 1: Normalize README baseline and frontend implementation direction

**Files:**
- Modify: `D:\code\my-infra\api-hub\README.md`
- Reference: `D:\code\my-infra\api-hub\prototype\package.json`
- Reference: `D:\code\my-infra\api-hub\prototype\src\App.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\marketing\LandingPage.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\docs\DocBrowserPage.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\console\ConsoleDashboard.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\console\ApiEditorPage.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\console\DebugPage.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\console\EnvironmentPage.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\console\VersionPage.tsx`
- Reference: `D:\code\my-infra\api-hub\prototype\src\pages\console\MockPage.tsx`

- [ ] **Step 1: Verify the current README still contains PostgreSQL-era assumptions**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern 'PostgreSQL|TIMESTAMPTZ|JSONB|pg_trgm|GIN|IDENTITY'
```
Expected: matches are returned, proving the README is still inconsistent with the MySQL decision.

- [ ] **Step 2: Verify the prototype routes that must be reflected in the frontend section**

Run:
```powershell
Get-Content 'D:\code\my-infra\api-hub\prototype\src\App.tsx'
```
Expected: route elements exist for `/`, `/login`, `/docs/:spaceKey/:projectKey`, `/console`, `/console/api-editor`, `/console/debug`, `/console/environments`, `/console/versions`, and `/console/mock`.

- [ ] **Step 3: Replace the README opening with a MySQL-first, single-node-first baseline and prototype-backed frontend strategy**

Write these sections near the top of `README.md`:
```md
# ApiHub Implementation README

## 1. 文档目标
- 说明 README 同时覆盖：MySQL DDL、REST API、服务边界、服务拆分、代码目录、核心类职责、消息流、缓存策略、迭代排期。

## 2. 技术基线
- MySQL 8 是唯一必选基础设施。
- Redis、RabbitMQ、OpenSearch、MinIO 都是可选增强组件。
- 默认部署形态是单机：Nginx + Next.js + Spring Boot + MySQL。
- 前端采用 Next.js 15 + React 19 + TypeScript + Tailwind CSS + shadcn/ui + Framer Motion。

## 3. 前端产品实现方向
- 明确原型页面映射：LandingPage -> 官网，DocBrowserPage -> 文档浏览页，ConsoleDashboard/ApiEditorPage/DebugPage/EnvironmentPage/VersionPage/MockPage -> 控制台核心工作台。
- 明确官网/浏览端使用 Raycast-like 卡片风，控制台继承设计语言但强调信息密度和编辑效率。
- 明确 prototype 只是参考起点，不是最终视觉标准；README 必须补充“高于当前原型精美程度”的目标描述。
- 明确控制台需要补齐分组管理、模块组织、接口树导航等原型尚未完整表达的能力。
```

- [ ] **Step 4: Verify the new baseline text is present**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern 'MySQL 8 是唯一必选基础设施|Next.js 15|LandingPage|DocBrowserPage|ConsoleDashboard'
```
Expected: each pattern is found exactly in the new README content.

- [ ] **Step 5: Commit the baseline rewrite checkpoint**

Run:
```bash
git add README.md
git commit -m "docs: rewrite readme baseline for mysql single-node architecture"
```

### Task 2: Replace PostgreSQL DDL with MySQL 8 compatible DDL

**Files:**
- Modify: `D:\code\my-infra\api-hub\README.md`

- [ ] **Step 1: Verify PostgreSQL-specific DDL tokens still exist before rewrite**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern 'TIMESTAMPTZ|JSONB|GENERATED BY DEFAULT AS IDENTITY|GIN|pg_trgm'
```
Expected: matches are returned.

- [ ] **Step 2: Replace PostgreSQL types and indexing assumptions with MySQL 8 compatible patterns**

Apply these rules throughout the DDL section:
```text
BIGINT GENERATED BY DEFAULT AS IDENTITY -> BIGINT NOT NULL AUTO_INCREMENT
TIMESTAMPTZ -> DATETIME(3)
JSONB -> JSON
GIN / pg_trgm indexes -> BTREE indexes or FULLTEXT indexes only where the README explicitly calls them out as optional search enhancement
CURRENT_TIMESTAMP -> CURRENT_TIMESTAMP(3)
```

- [ ] **Step 3: Rewrite the core DDL snippets so they read like valid MySQL 8 DDL**

Ensure representative table definitions follow this shape:
```sql
CREATE TABLE api_endpoint (
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    project_id BIGINT NOT NULL,
    module_id BIGINT NOT NULL,
    group_id BIGINT NULL,
    name VARCHAR(256) NOT NULL,
    summary VARCHAR(512) NULL,
    description TEXT NULL,
    route_key VARCHAR(640) NOT NULL,
    http_method VARCHAR(16) NOT NULL,
    path VARCHAR(512) NOT NULL,
    content_type VARCHAR(128) NULL,
    status VARCHAR(16) NOT NULL DEFAULT 'draft',
    deprecated TINYINT(1) NOT NULL DEFAULT 0,
    source_type VARCHAR(16) NOT NULL DEFAULT 'manual',
    version_label VARCHAR(32) NULL,
    sort_order INT NOT NULL DEFAULT 0,
    is_locked TINYINT(1) NOT NULL DEFAULT 0,
    locked_by BIGINT NULL,
    current_revision_id BIGINT NULL,
    published_revision_id BIGINT NULL,
    created_by BIGINT NOT NULL,
    updated_by BIGINT NOT NULL,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    UNIQUE KEY uk_api_endpoint_route (project_id, route_key),
    KEY idx_api_endpoint_module_sort_order (module_id, sort_order),
    KEY idx_api_endpoint_status_updated_at (status, updated_at),
    CONSTRAINT fk_api_endpoint_project FOREIGN KEY (project_id) REFERENCES project (id),
    CONSTRAINT fk_api_endpoint_module FOREIGN KEY (module_id) REFERENCES module (id),
    CONSTRAINT fk_api_endpoint_group FOREIGN KEY (group_id) REFERENCES api_group (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```
The same MySQL style must be applied consistently to `api_parameter`, `api_response`, `api_version`, `module_version_tag`, `dict_group`, `dict_item`, `environment`, `mock_rule`, `test_suite`, `test_case`, `test_step`, `test_execution`, `debug_history`, `doc_push_record`, `share_link`, `audit_log`, and `ai_task`.

- [ ] **Step 4: Add a short MySQL note for search and advanced capabilities**

Insert this note after the DDL section:
```md
说明：首版仅依赖 MySQL 8。全文搜索默认使用 MySQL 基础检索能力；当搜索规模和相关性要求提高时，可选接入 OpenSearch 作为增强组件。
```

- [ ] **Step 5: Verify PostgreSQL-specific tokens are gone**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern 'TIMESTAMPTZ|JSONB|GENERATED BY DEFAULT AS IDENTITY|GIN|pg_trgm'
```
Expected: no output.

- [ ] **Step 6: Commit the DDL rewrite checkpoint**

Run:
```bash
git add README.md
git commit -m "docs: convert readme ddl section to mysql 8"
```

### Task 3: Expand README into a complete implementation document

**Files:**
- Modify: `D:\code\my-infra\api-hub\README.md`

- [ ] **Step 1: Verify the README still lacks the final implementation sections**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern '^## .*服务拆分方案|^## .*代码目录|^## .*核心类职责|^## .*消息流|^## .*缓存策略|^## .*迭代排期'
```
Expected: missing or incomplete matches before this task starts.

- [ ] **Step 2: Add a dedicated service split section**

Insert a section with this exact structure:
```md
## X. 服务拆分方案
- apihub-web：Next.js 应用，承载官网、分享页、文档浏览页、控制台
- apihub-server：Spring Boot 模块化单体
- 可选增强：独立 mock / runner / ai-worker / indexer-worker
- 明确首版运行时只有 web + server + mysql + nginx
```

- [ ] **Step 3: Add a code directory section tied to the approved architecture**

Insert a section with this exact directory skeleton:
```text
apps/
  web/
packages/
  ui/
  api-sdk/
  config/
services/
  apihub-server/
    src/main/java/com/apihub/
      common/
      auth/
      space/
      project/
      doc/
      dict/
      env/
      debug/
      mock/
```
and explain each top-level directory in one sentence. The explanation for console-facing directories must mention module grouping, endpoint grouping, and documentation tree organization as first-class product areas.

- [ ] **Step 4: Add a core class responsibility section**

Insert bullets covering these exact classes/services:
```text
AuthController
AuthService
PermissionService
ProjectController
ModuleController
ApiGroupController
ApiEndpointController
ApiSchemaController
ApiVersionController
ApiEndpointService
ApiSchemaService
ApiVersionService
ApiDiffService
DocPushService
EnvironmentService
EnvironmentSecretService
DebugService
RequestTemplateResolver
MockRuleService
MockRuntimeService
AuditLogService
```
Each item must state what it owns and what it must not own.

- [ ] **Step 5: Add the message flow and cache strategy sections**

The message flow section must cover these five flows:
```text
人工维护接口
DocForge 推送
接口发布
调试执行
Mock 命中
```
The cache strategy section must explicitly say:
```text
首版默认使用 Caffeine
没有 Redis 也必须能跑
Redis 仅作为二级缓存增强
```

- [ ] **Step 6: Add the roadmap section with four phases**

Insert the roadmap with these exact phase names and scope:
```text
Phase 1：MVP 基础闭环
Phase 2：Mock 与发布增强
Phase 3：体验与性能增强
Phase 4：二期预留能力
```
Each phase must include goals and acceptance criteria. Phase 1 or Phase 3 must explicitly mention upgrading the frontend quality beyond the current prototype and filling missing product capabilities such as grouping management and richer console information architecture.

- [ ] **Step 7: Verify the implementation sections are all present**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern '服务拆分方案|代码目录|核心类职责|消息流|缓存策略|迭代排期|Caffeine|Phase 1：MVP 基础闭环|Phase 4：二期预留能力'
```
Expected: all patterns are found in the rewritten README.

- [ ] **Step 8: Commit the completed implementation rewrite**

Run:
```bash
git add README.md
git commit -m "docs: expand readme into mysql implementation guide"
```

### Task 4: Final validation against spec and prototype

**Files:**
- Modify: `D:\code\my-infra\api-hub\README.md`
- Reference: `D:\code\my-infra\api-hub\docs\superpowers\specs\2026-04-08-apihub-mysql-single-node-design.md`

- [ ] **Step 1: Validate the README against the approved spec themes**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern 'MySQL 8|单机|Next.js 15|React 19|Tailwind CSS|shadcn/ui|Framer Motion|Mock|AI|测试编排|仅 MySQL 为必选基础设施'
```
Expected: all approved constraints appear in the README.

- [ ] **Step 2: Validate the prototype page coverage is explicitly documented**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern 'LandingPage|DocBrowserPage|ApiEditorPage|DebugPage|EnvironmentPage|VersionPage|MockPage'
```
Expected: all prototype page names or their exact mapped responsibilities appear in the frontend implementation section, plus explicit wording that the final product must exceed the current prototype's polish and fill missing grouping/module-management capabilities.

- [ ] **Step 3: Perform a final placeholder scan**

Run:
```powershell
Select-String -Path 'D:\code\my-infra\api-hub\README.md' -Pattern 'TODO|TBD|占位|待定|稍后|后续补充'
```
Expected: no output.

- [ ] **Step 4: Review the final git diff for scope correctness**

Run:
```bash
git diff -- README.md
```
Expected: only the README rewrite is shown; no unrelated file changes are mixed into this implementation step.

- [ ] **Step 5: Commit the final polished README**

Run:
```bash
git add README.md
git commit -m "docs: finalize mysql-first apihub readme"
```
